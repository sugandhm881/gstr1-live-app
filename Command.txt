# Now run the main sequence
cd GSTR1_Final_App
git add .
git commit -m "Initial commit: Add application files"
git branch -M main
git pull origin main --allow-unrelated-histories
git push -u origin main